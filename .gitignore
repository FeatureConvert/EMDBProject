# Python
.venv/
__pycache__/
*.pyc

# Per-deposition working state - contains real sample metadata, dep_ids,
# and local file paths a researcher may not want committed.
depositions/

# Rendered/design PDFs kept locally, not committed - README.pdf duplicates
# README.md, and these are large binaries that bloat the repo.
*.pdf

# OS cruft
.DS_Store

# Claude Code harness session state - not project content
.claude/scheduled_tasks.lock
